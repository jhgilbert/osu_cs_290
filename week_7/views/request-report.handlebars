<h1>{{requestType}} request report</h1>

<h2>Query Parameters</h2>

{{#unless queryParams}}
<p>This request did not have query parameters.</p>
{{/unless}}

<ul>
  {{#each queryParams}}
  <li><code>{{@key}}</code> had a value of <code>{{this}}</code>.</li>
  {{/each}}
</ul>

{{#if bodyParams}}
<h2>Body Parameters</h2>

<ul>
  {{#each bodyParams}}
  <li><code>{{@key}}</code> had a value of <code>{{this}}</code>.</li>
  {{/each}}
</ul>
{{/if}}
